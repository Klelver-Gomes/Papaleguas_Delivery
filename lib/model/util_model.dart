import 'dart:ui';

import 'package:flutter/material.dart';

  const COLOR_TEXT = Colors.white;
  const COLOR_BUTTON = Color.fromRGBO(49, 26, 136, 1);
  const COLOR_BUTTON_CANCEL = Colors.red;
  const COLOR_TEXTFIELD = Color.fromRGBO(49, 26, 136, 1);
  const COLOR_BACKGROUND = Color.fromRGBO(217, 217, 217, 1);
  const COLOR_CARD = Color.fromRGBO(120, 120, 120, 1);
  const COLOR_ICON = Color.fromRGBO(254, 186, 0, 1);
